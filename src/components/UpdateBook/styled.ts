import styled from '@emotion/styled';
import { Modal } from 'antd';

export const StyledUpdateBook = styled.div`
   
`;

export const StyledModal = styled(Modal)`
 
  Input {
    margin-bottom: 10px;
  }
`;
